import 'package:flutter/material.dart';

class FlutterFragment extends StatelessWidget {
  const FlutterFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Flutter fragment'),),
    );
  }
}